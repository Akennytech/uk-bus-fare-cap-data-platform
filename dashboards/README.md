# Dashboards

Add your Power BI `.pbix` files here once built (Week 7 of the roadmap), plus
exported PNG screenshots of each page for the README and LinkedIn posts.

Planned pages (see the full project plan document, Section 12, for the
detailed spec of each):

1. Executive Overview
2. Cost of Living
3. Route & Operator Performance
4. Accessibility
5. Environmental Impact

Connect Power BI Desktop directly to the PostgreSQL `gold` schema
(Get Data -> PostgreSQL database -> `localhost:5432` / `bus_fare_cap`).
