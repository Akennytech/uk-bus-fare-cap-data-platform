name: 'uk_bus_fare_cap'
version: '1.0.0'
config-version: 2

profile: 'uk_bus_fare_cap'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  uk_bus_fare_cap:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: gold
